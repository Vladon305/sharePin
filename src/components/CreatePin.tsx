import React from 'react'
import { User } from '../types/types'

type PropsType = {
  user?: User | null
}

const CreatePin: React.FC<PropsType> = () => {
  return (
    <div>CreatePin</div>
  )
}

export default CreatePin