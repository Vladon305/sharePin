import React from 'react'
import { User } from '../types/types'

type PropsType = {
  user?: User | null
}

const PinDetail: React.FC<PropsType> = () => {
  return (
    <div>
      PinDetail
    </div>
  )
}

export default PinDetail
